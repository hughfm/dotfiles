[pager]
diff = diff-so-fancy | less --tabs=4 -RFX
show = diff-so-fancy | less --tabs=4 -RFX

[color]
ui = auto

[push]
  default = simple

[pull]
  rebase = true

[rebase]
  autoSquash = true
  autoStash = true

[include]
  path = ~/.gitconfig_private
